# Default values for localservice.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

image:
  repository: nginx
  tag: stable
  pullPolicy: IfNotPresent

targetPort:
pathPart:
vmToHostIp:
